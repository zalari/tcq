# The manifest for the "tcq" service.
# Read the full specification for the "Load Balanced Web Service" type at:
#  https://aws.github.io/copilot-cli/docs/manifest/lb-web-service/

# Your service name will be used in naming your resources like log groups, ECS services, etc.
name: tcq
type: Load Balanced Web Service

# Distribute traffic to your service.
http:
  # Requests to this path will be forwarded to your service.
  # To match all requests you can use the "/" path.
  path: '/'
  # You can specify a custom health check path. The default is "/".
  # healthcheck: '/'

# Configuration for your containers and service.
image:
  # Docker build arguments. For additional overrides: https://aws.github.io/copilot-cli/docs/manifest/lb-web-service/#image-build
  build: service.Dockerfile
  # Port exposed through your container to route traffic to it.
  port: 3000

cpu: 1024       # Number of CPU units for the task.
memory: 2048    # Amount of memory in MiB used by the task.
platform: linux/arm64  # See https://aws.github.io/copilot-cli/docs/manifest/lb-web-service/#platform
count: 1       # Number of tasks that should be running in your service.
exec: true     # Enable running commands in your container.
network:
  connect: true # Enable Service Connect for intra-environment traffic between services.

# storage:
  # readonly_fs: true       # Limit to read-only access to mounted root filesystems.

# Optional fields for more advanced use-cases.
#
#variables:                    # Pass environment variables as key value pairs.
#  LOG_LEVEL: info
variables:                    # Pass environment variables as key value pairs.
  TCQ_GH_CALLBACK_URL: https://tcq.${COPILOT_ENVIRONMENT_NAME}.tcq-reloaded.tcq-reloaded.com/auth/github/callback
  TCQ_DB_ADAPTER: dynamodb
  TCQ_SESSION_STORE_ADAPTER: dynamodb
  TCQ_DB_DYNAMODB_MEETINGS_TABLE:
    from_cfn: ${COPILOT_APPLICATION_NAME}-${COPILOT_ENVIRONMENT_NAME}-meetingsTableName
  TCQ_SESSION_STORE_DYNAMODB_TABLE:
    from_cfn: ${COPILOT_APPLICATION_NAME}-${COPILOT_ENVIRONMENT_NAME}-sessionsTableName

secrets:                      # Pass secrets from AWS Systems Manager (SSM) Parameter Store.
  TCQ_GH_SECRET: /copilot/${COPILOT_APPLICATION_NAME}/${COPILOT_ENVIRONMENT_NAME}/secrets/TCQ_GH_SECRET
  TCQ_SESSION_SECRET: /copilot/${COPILOT_APPLICATION_NAME}/${COPILOT_ENVIRONMENT_NAME}/secrets/TCQ_SESSION_SECRET
  TCQ_GH_ID: /copilot/${COPILOT_APPLICATION_NAME}/${COPILOT_ENVIRONMENT_NAME}/secrets/TCQ_GH_ID
# uncomment the following lines to configure mongoDB URIs, if enabled as either db or session store
# ...and make sure to set the secrets in SSM Parameter Store via copilot CLI with `copilot secret init`
#  TCQ_DB_MONGODB_URI: /copilot/${COPILOT_APPLICATION_NAME}/${COPILOT_ENVIRONMENT_NAME}/secrets/TCQ_DB_MONGODB_URI
#  TCQ_SESSION_STORE_MONGODB_URI: /copilot/${COPILOT_APPLICATION_NAME}/${COPILOT_ENVIRONMENT_NAME}/secrets/TCQ_DB_MONGODB_URI

#faster deployments
deployment:
  rolling: 'recreate'

# You can override any of the values defined above by environment.
environments:
  production:
    http:
      path: '/'
      alias: tcq-reloaded.com
    variables:
      TCQ_GH_CALLBACK_URL: https://tcq-reloaded.com/auth/github/callback
  staging:
    http:
      path: '/'
      alias: staging.tcq-reloaded.com
    variables:
      TCQ_GH_CALLBACK_URL: https://staging.tcq-reloaded.com/auth/github/callback

#  test:
#    count: 2               # Number of tasks to run for the "test" environment.
#    deployment:            # The deployment strategy for the "test" environment.
#       rolling: 'recreate' # Stops existing tasks before new ones are started for faster deployments.
